/** @deprecated Import from `./mindmapClipboard` — kept so Vite stale graphs do not 404. */
export {
  pasteCanvasOutline,
  readMindmapClipboard,
  writeMindmapClipboard,
} from './mindmapClipboard'
